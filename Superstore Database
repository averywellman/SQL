#averywellman's SQL Portfolio

##Welcome to my SQL Portfolio! This code respository will demonstrate examples of SQL's I have written. 

SELECT *
FROM superstore
ORDER BY price;

SELECT SUM(price)
FROM superstore;

SELECT AVG(price)
FROM superstore;

SELECT MIN(price)
FROM superstore;

SELECT MAX(price)
FROM superstore;

SELECT COUNT(price)
FROM superstore;

SELECT item_name, price
FROM superstore
WHERE category = "Kitchen Supplies";

SELECT stock_quantity
FROM superstore
WHERE item_name = "Air Purifier";

SELECT item_name, MAX(stock_quantity)
FROM superstore;

SELECT item_name, MIN(stock_quantity)
FROM superstore;
